AA(x, y) -> int(x), int(y).
DD(x, y) -> int(x), int(y).
BB(x, y) -> int(x), int(y).
ZZ(x, y) -> int(x), int(y).


YY(x, y) -> int(x), int(y).
XX(x, y) -> int(x), int(y).

YY(x, y) <- DD(x, z), !ZZ(y, z).
XX(x, y) <- YY(x, z), BB(y, z).
